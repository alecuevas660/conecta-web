---
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title?: string;
}

const { title = "Nosotros - Conecta Internacional" } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  </head>
  <body>
    <Navbar />
    
    <main>
      <!-- Hero Section -->
      <section class="bg-light py-5 text-center">
        <div class="container">
          <h1 class="display-4 fw-bold mb-4">Sobre Nosotros</h1>
          <p class="lead">Conectando el mundo con soluciones innovadoras de comunicación</p>
        </div>
      </section>

      <!-- Nuestra Historia Section -->
      <section class="py-5">
        <div class="container">
          <div class="row align-items-center">
            <div class="col-lg-6 mb-4 mb-lg-0">
              <img src="/placeholder.svg?height=400&width=600" alt="Nuestra Historia" class="img-fluid rounded">
            </div>
            <div class="col-lg-6">
              <h2 class="fw-bold mb-4">Nuestra Historia</h2>
              <p>Desde nuestra fundación en [año], Conecta Internacional ha estado a la vanguardia de las soluciones de comunicación global. Comenzamos con la visión de crear un mundo más conectado y hemos crecido hasta convertirnos en líderes en servicios de telecomunicaciones internacionales.</p>
              <p>Nuestro compromiso con la innovación y la excelencia nos ha permitido expandir nuestros servicios y alcance, siempre manteniendo nuestro enfoque en la satisfacción del cliente y la calidad del servicio.</p>
            </div>
          </div>
        </div>
      </section>

      <!-- Nuestros Valores Section -->
      <section class="bg-light py-5">
        <div class="container">
          <h2 class="text-center fw-bold mb-5">Nuestros Valores</h2>
          <div class="row g-4">
            {['Innovación', 'Integridad', 'Colaboración', 'Excelencia'].map((valor) => (
              <div class="col-md-3">
                <div class="card h-100 border-0 shadow-sm">
                  <div class="card-body text-center">
                    <i class={`bi bi-${valor.toLowerCase() === 'innovación' ? 'lightbulb' : 
                                 valor.toLowerCase() === 'integridad' ? 'shield-check' :
                                 valor.toLowerCase() === 'colaboración' ? 'people' : 'star'} 
                               fs-1 text-primary mb-3`}></i>
                    <h3 class="card-title h5 fw-bold">{valor}</h3>
                    <p class="card-text">Descripción breve del valor y cómo lo aplicamos en nuestro trabajo diario.</p>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      <!-- Nuestro Equipo Section -->
      <section class="py-5">
        <div class="container">
          <h2 class="text-center fw-bold mb-5">Nuestro Equipo</h2>
          <div class="row g-4">
            {[
              { name: 'Ana García', position: 'CEO' },
              { name: 'Carlos Rodríguez', position: 'CTO' },
              { name: 'Laura Martínez', position: 'Directora de Operaciones' },
              { name: 'Miguel Sánchez', position: 'Director de Marketing' }
            ].map((member) => (
              <div class="col-md-3">
                <div class="card h-100 border-0 shadow-sm">
                  <img src="/placeholder.svg?height=300&width=300" alt={member.name} class="card-img-top">
                  <div class="card-body text-center">
                    <h3 class="card-title h5 fw-bold">{member.name}</h3>
                    <p class="card-text">{member.position}</p>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      <!-- Call to Action Section -->
      <section class="bg-primary text-white text-center py-5">
        <div class="container">
          <h2 class="fw-bold mb-4">¿Listo para conectarte con nosotros?</h2>
          <p class="lead mb-4">Descubre cómo podemos ayudarte a alcanzar tus objetivos de comunicación global.</p>
          <a href="/contacto" class="btn btn-light btn-lg">Contáctanos</a>
        </div>
      </section>
    </main>

    <Footer />

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>

